# 第三方来源说明

本仓库的主体代码基于 `cmliu/edgetunnel`，继续遵循仓库内的 GPL-3.0
许可证。

公开地址池读取
`LancelotRar/best-cf-ips` 公开生成的 `best-cf-ipv4.txt`。本项目只读取该项目
对外发布的数据格式，并自行实现验证、去重、缓存和节点命名；没有复制该仓库
脚本代码。

`LancelotRar/free-subs` 仅用于了解公开订阅的整体使用方式。本项目没有复制其
订阅内容、工作流、路由规则或 `src/example.yaml`。
